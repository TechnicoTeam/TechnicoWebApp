@model RepairsViewModel
@using Technico.Main.Models.Enums
@using Technico.Main.DTOs.RepairDtos
@using Technico.Main.DTOs


@{
    ViewData["Title"] = "Repairs";
}


<div class="container col-md-8">

    <div class="card shadow-sm my-4">
        <div class="card-header d-flex text-white bg-primary justify-content-between mb-3">
            <h3 class="card-title"><i class="fa-sharp fa-solid fa-hammer"></i> Repairs</h3>
            <div style="width:250px;" class="d-flex justify-content-end">
                <a class="btn btn-primary" asp-action="Create">Create new Repair</a>
            </div>
        </div>
        <div class="card-body p-4">

            <div class="px-5">
                <form >
                    <div class="row mb-4">
                        <div class="col-md-6 mx-auto">
                            <input type="text" class="form-control" value="" placeholder="Search by owners vat" name="searchVat" id="searchVat" aria-label="Search" />
                        </div>
                    </div>

                    <hr />
                    <div class="d-flex gap-4">
                        <h6 class="my-auto"><i class="fa-solid fa-filter"></i> Filters</h6>
 
                        <div class="col-3">
                            <select id="searchStatus" class="form-select" name="searchStatus">
                                <option selected hidden> Status </option>
                                <option value="@StatusOfRepair.Pending"> @StatusOfRepair.Pending </option>
                                <option value="@StatusOfRepair.In_progress"> @StatusOfRepair.In_progress.ToString().Replace("_"," ") </option>
                                <option value="@StatusOfRepair.Complete"> @StatusOfRepair.Complete </option>
                            </select>
                        </div>


                        <div class="col-3">
                            <input type="date" class="form-control" name="searchFromDate" id="searchFromDate" placeholder="dd-mm-yyyy" />
                        </div>
                        <div class="col-3">
                            <input type="date" class="form-control" name="searchToDate" id="searchToDate" placeholder="dd-mm-yyyy" />
                        </div>
                    </div>
                    <hr />
                </form>
               

            </div>

            <div class="mb-3 table-responsive">
                <table class="table table-striped table-hover table-bordered align-middle">
                    <thead class="table-dark sticky-top">
                        <tr class="text-center">
                            <th class="align-middle">Vat</th>
                            <th class="align-middle">E9</th>
                            <th class="align-middle">Type</th>
                            <th class="align-middle">Status</th>
                            <th class="align-middle">Cost</th>
                            <th class="align-middle">CreatedAt</th>
                            <th class="align-middle">Actions</th>
                        </tr>
                    </thead>
                    <tbody class="table-light" id="RepairTableBody">
                        @if (!Model.Repairs.Any())
                        {
                            <tr class="repairs-row">
                                The are not any repair for this property.
                            </tr>
                        }
                        else
                        {
                            @foreach (var r in Model.Repairs)
                            {
                                <tr class="repairs-row">
                                    <td>@foreach(var owner in r.Property.Owners)
                                        {
                                            @owner.Vat<br />
                                        }
                                    </td>
                                    <td>@r.Property.E9</td>
                                    <td class="align-middle">@r.Type.ToString().Replace("_", " ")</td>
                                    <td class="align-middle">@r.Status.ToString().Replace("_", " ")</td>
                                    <td class="align-middle">@r.Cost</td>
                                    <td class="align-middle">@r.CreatedAt</td>
                                    <td class="text-center align-middle">

                                        <div class="btn-group" role="group">
                                            <button class="btn btn-outline-primary btn-sm me-2 rounded-pill" data-bs-toggle="tooltip" title="Edit Repair">
                                                <i class="fas fa-cog"></i>
                                            </button>

                                            <button class="btn btn-outline-danger btn-sm" data-bs-toggle="tooltip" title="Delete Repair">
                                                <i class="fas fa-trash"></i>
                                            </button>

                                        </div>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>


        </div>
    </div>

</div>